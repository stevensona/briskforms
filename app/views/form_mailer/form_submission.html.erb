<h1>New submission from your Brisk Form</h1>

<h2>Contents</h2>

<% @contents.each do |k, v| %>
<% if k != 'controller' and k != 'action' and k != 'id' %>
<b><%= k %></b> - <%= v %><br>
<% end %>
<% end %>

<h2>Administration</h2>
<p>Details for this form handler, click <a href="<%= "https://briskforms.com/show/#{@form.admin_url}" %>">here</a>. To stop receiving these emails, delete the form submission handler by clicking <a href="<%= "http://briskforms.com/delete/#{@form.admin_url}" %>">here</a>.</p>
<p>Thanks for using Brisk Forms to process form submissions!</p>

-Brisk Forms
